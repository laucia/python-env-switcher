{
  "name": "python-env-switcher",
  "displayName": "Python Env Switcher",
  "description": "Automatically switch to the relevant python virtual environment depending on the active file. Supports the Python Environment extension.",
  "version": "0.0.1",
  "publisher": "laucia",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/laucia/python-env-switcher.git"
  },
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:python"
  ],
  "extensionDependencies": [
    "ms-python.vscode-python-envs"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "Python Env Switcher",
      "properties": {
        "pythonEnvSwitcher.venvName": {
          "type": "string",
          "default": ".venv",
          "description": "Name of the virtual-environment directory to look for when walking up from the active file."
        },
        "pythonEnvSwitcher.scope": {
          "type": "string",
          "enum": ["workspaceFolder", "project", "file"],
          "default": "workspaceFolder",
          "markdownDescription": "Scope passed to `setEnvironment`. **`workspaceFolder`** sets the env for the whole workspace folder — the only scope Pylance's single per-folder analysis service reads, so it is what actually moves Pylance (it flips the whole folder's analysis to the active file's venv). `project` sets it on the file's containing Python project (which Pylance ignores for sub-projects in a single-folder window). `file` sets it per file."
        },
        "pythonEnvSwitcher.showNotifications": {
          "type": "boolean",
          "default": false,
          "description": "Show an information message each time the environment is switched."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.125.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.56.1",
    "eslint": "^9.39.3",
    "esbuild": "^0.27.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
